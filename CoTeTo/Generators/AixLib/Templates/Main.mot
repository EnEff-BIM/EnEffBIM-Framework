within AixLib;
model UseCase

% for key, value in MapData.buildings[-1].hvac_component_group.items():
%for i, item in enumerate(value):
${item.target_location} ${item.target_name};
%endfor
%endfor

% for key, value in MapData.buildings[-1].thermal_zones[-1].hvac_component_group.items():
%for i, item in enumerate(value):
${item.target_location} ${item.target_name};
%endfor
%endfor
equation
% for item in MapData.connections:
connect(${item.connector_a.parent.target_name}.${item.connector_a.name}, ${item.connector_b.parent.target_name}.${item.connector_b.name});
%endfor
end UseCase;