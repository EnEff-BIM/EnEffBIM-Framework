## Main controller file
## call subfiles from here

within ;
model ${modelName | trim} "${modelName}"

% for key, value in comDict.items() :
  ${value} ${key} "documentation string";
% endfor

equation
// connect() equations only

annotation(uses( Modelica(version = "3.2.1"), AixLib(version = "0.1.0")));
end ${modelName | trim};
